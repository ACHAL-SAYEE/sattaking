<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/css/sb-admin-2.min.css" rel="stylesheet" />
    <title>Document</title>
  </head>
  <style>
    * {
      box-sizing: border-box;
      margin: 0px;
      padding: 0px;
    }
    .login-bg {
      background: radial-gradient(
        circle,
        rgba(2, 0, 36, 1) 0%,
        rgba(38, 37, 69, 1) 25%,
        rgba(52, 52, 82, 1) 35%,
        rgba(122, 84, 40, 1) 50%,
        rgba(0, 212, 255, 1) 100%
      );
      height: 100vh;
      width: 100vw;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-box {
      background-color: white;
      width: 400px;
      min-height: 250px;
      display: flex;
      justify-content: center;
      align-items: center;
      border-radius: 10px;
      padding: 10px;
    }
    @media (max-width: 400px) {
      .login-box {
        width: 90%;
      }
    }
    .login-form {
      display: flex;
      flex-direction: column;
    }
    .con {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
    }
    form {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      /* justify-content: center; */
      /* padding: 10px; */
      width: 70%;
      /* border: solid red; */
    }
    form div {
      width: 100%;
    }
    input {
      width: 100%;
      margin-bottom: 10px;
      height: 40px;
    }
    label {
      margin-bottom: 5px;
    }
  </style>
  <body>
    <div class="login-bg">
      <div class="login-box">
        <form class="login-form" action="/reset-password?token=<%= token %>" method="POST">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <input type="hidden" name="token" value="<%= token %>" />

          <div class="con">
            <label for="password">password</label>
            <input id="password" name="password" required  placeholder="enter new password" />
          </div>

          <div class="con">
            <label for="confirmPassword">Confirm Password</label>
            <input id="confirmPassword" name="confirmPassword" required  placeholder="reenter your password" />
          </div>
         
          <%if(locals.error){ %>
          <span style="color: red; margin-bottom: 10px">* <%= locals.error %></span>
          <% }%>
         
          <button class="btn btn-primary">Reset</button>
        </form>
      </div>
    </div>
  </body>
</html>
